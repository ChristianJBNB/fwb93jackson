extends layout

block content
  h1= title
    - var image_names = ["durant.jpg", "jokic.jpg", "lebron.jpg", "luka.jpg", "zion.jpg"]
      - for(i=0; i < 3; i++){
        - rand_img= Math.floor(Math.random()*5)
        img(src= `/stylesheets/${image_names[rand_img]}`)
      -}
         
